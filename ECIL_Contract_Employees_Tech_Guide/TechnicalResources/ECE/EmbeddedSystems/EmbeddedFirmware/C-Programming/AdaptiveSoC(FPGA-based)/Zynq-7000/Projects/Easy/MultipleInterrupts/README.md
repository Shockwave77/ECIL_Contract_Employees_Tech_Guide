# Problem Statement

Implement a system that handles multiple external interrupt sources using the Zynq Generic Interrupt Controller (GIC). The application detects interrupt events generated by slide switches and push buttons connected through AXI GPIO and executes interrupt service routines to read and print the input values.
